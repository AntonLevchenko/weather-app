import { ICurrentWeatherState } from '../interfaces/current-weather.state.interface';

export const CURRENT_WEATHER_FEATURE_KEY = 'currentWeather';

export const INITIAL_CURRENT_WEATHER_STATE: ICurrentWeatherState = {
  weatherData: null
};
